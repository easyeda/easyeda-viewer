



V 目前pcb图层渲染的视觉效果是底层在上面了，应该是顶层在顶层，内层，底层的视觉效果层级的。
 钻孔 -> 板框 ->顶层丝印 -> 顶层 -> 顶层阻焊 - > 顶层助焊 -> 顶层装配 -> 内层1/2/3... ->  底层丝印 -> 底层 -> 底层阻焊 - > 底层助焊 ->底层装配 -> 机械层、文档层、自定义层、引脚相关层、元件相关层、3D相关层

V PCB元件的位号显示需要根据PCB上的位号属性是否显示呈现，目前是全部显示了

V 视觉上阻焊层在铜层后面渲染，焊盘的阻焊需要根据设计规则和焊盘的自定义扩展属性外扩形状进行显示，透明度按PCB的数据来

V 拖动epro2和zip加载报错 加载失败:无法识别的工程格式（支持 .eprj3 目录/.zip、.epro2、单个 .esch2/.epcb2/.epan2/.epru）

V 右侧图层列表的展示顺序需要调整顺序以人使用需要排序，不需要和渲染逻辑相同排序

V Top Paste Mask Layer有些焊盘没有设置助焊扩展数据的，需要取设计规则里面的全部参数渲染（焊盘形状的外扩，一般默认是0）

V Top Paste Mask Layer和bottom paste mask layer没有区分开，全显示在top面去了

V 右侧图层列表的层名需要做一下翻译，在格式枚举内的层名可翻译的都翻译

V 焊盘和导线的网络名需要显示在图元内部，自动调整文本宽度高度不超出图元外部

V 底层焊盘底层封装的焊盘编号也跟随镜像翻转了不利于阅读，需要正向显示或竖向显示；顶层封装如果旋转了角度，焊盘编号文本也旋转了，需要正向显示

V 相同层的焊盘和导线和铺铜和填充的，焊盘应该在视觉上在他们上面

V 有很多焊盘和导线无法显示网络名，eda中是可以正常显示出来的，只有很小的无法显示

V 根据PCB里面存储的网络颜色，把对应相同网络的图元设置对应的颜色

V 顶层网络名在右侧图层列表排序需要放在顶层焊盘编号下面

V 图层列表需要支持点击高亮，点击对应的实体图元的层就把这个层临时提到最前面作为活跃层，没有实体图元的层优先级不变

V 槽孔图元没有渲染出来。samples\ESP32S31-epro2\ESP32S31\立创·Candis-ESP32S31开发板_copy.epru的PCB的 USB2 X / Y	155, 252.265；USB2整体在底层镜像反了180度。CN2元件的丝印层喇叭 logo也镜像反了；U26也镜像反了；有些底层封装却正常

V PCB铺铜开启了制造优化后是有导线包边的，目前查看没有发现包边导线的渲染；在eda中如果开启了铺铜制造优化后，会使用8mil圆头导线包住铺铜填充的边沿。

V 中心 X 121.4mil 中心 Y 1457.3mil 附近的几个焊盘位置又不对了；

V USB1的多层焊盘的槽孔方向又不对了；

V 封装内的挖槽区域 SLOT REGION图元还是没有渲染出来

V 铺铜包边线条已经显示，但是通明度需要和铺铜的透明度一致

V 焊盘有网络名的时候需要同时把焊盘编号和网络名显示出来（网络名层开启），目前只看到了焊盘编号。

V 相同铜层时，顶层焊盘需要视觉上叠在顶层导线上面，底层同理，切到内层时，多层焊盘也是需要在导线上面