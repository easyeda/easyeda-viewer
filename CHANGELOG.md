# 更新日志(Changelog)

本项目的所有显著变更按版本记录于此。格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本遵循语义化版本;**此后每次版本号更新须在此补充条目**。

提交级细节见 git log 与 [docs/PRD.md](PRD.md) 的需求条目(`#tag` 可在 PRD 内全文检索)。

## [0.4.0] - 2026-09-21

### Added
- 建立本 CHANGELOG:此后每次版本号更新补充条目。

### Added
- PCB 族文档打开时激活层默认设为顶层铜皮(#default-top-layer):图层面板行同步高亮,默认激活只落拾取优先层、不改写初始层叠;重置/切文档后重开仍默认顶层。
- PCB 文档中封装内部焊盘可点击拾取(#fp-pad-pick):点焊盘选中焊盘并展示焊盘号/形状/宽高/钻孔/网络,点封装内部空白选中整个封装;通孔焊盘拾取归属元件面层。

### Fixed
- 单位切换按钮改为显示当前单位(此前显示目标单位),样式加强为淡底描边常亮 + 悬停实底(#unit-toggle 用户反馈);属性面板单位联动加 shell 直接刷新兜底。
- 量测模式下再次点击工具栏量测按钮 = 清空全部标尺并退出(此前仅退出保留标尺;Esc 仍保留标尺)。
- 原理图大字号文本位置偏移(ESP32S31-epro2 图页 ESP32-S31 的 RF-Antenna 等)。
- 删除 sch.ts 中重复的 `ATTR` case 死子句,消除 `vite build` 警告。

## [0.3.0] - 2026-09-20

> 本包版本区间内按 PRD 批次陆续交付(v0.2.1 ~ v0.3.5),包版本号在此期间停在 0.3.0。

### Added
- **桌面交付(v0.2.3)**:exe 产物名带版本号 `easyeda-viewer_v{version}.exe`、窗口标题栏版本、WebView2 缺失原生弹官方下载提示、HTML favicon 内联 exe 同款 .ico;`node scripts/build-desktop.mjs` 交付链。
- **量测工具(v0.3.2 #measure)**:PCB/封装/面板文档三角形标尺(斜边=测量线、直角边等间隔刻度 1/2/5×10ⁿ 档)、满屏十字光标线、|ΔX|/|ΔY|/距离标注;白色 1px 主线双描增强 + 暗晕衬底,屏幕空间绘制恒定不随缩放;右键=清标尺退出、Esc/按钮=退出留标尺、切文档自动清空。
- **顶栏 mm/mil 单位切换(v0.3.3 #unit-toggle)**:工具栏按钮切换并持久化(localStorage `ev.unit`),状态栏坐标/属性面板尺寸钻孔/量测读数与刻度全部联动;换算按文档族(PCB/封装 1 单位=1mil,原理图/面板 1 单位=0.254mm=10mil),修正此前 sch 体系按 mil 标注的 10× 错标。
- **加载中忙碌遮罩(v0.2.5 #loading)**:覆盖工程解析与文档切换,阻塞前双帧让渡保证先显示。
- 封装文档点焊盘直接展示焊盘号/形状/宽高/钻孔(v0.3.4)。

### Fixed
- **渲染保真批次(v0.2.1)**:以官方 PNG 导出为基准逐页对照修偏 —— 边框/标题栏按文件属性、电源符号方向、顺时针旋转角、网络标签 0.4em 抬升、文本拾取 bbox 画布实测、引脚标签对齐、隐藏文档/标题栏、选中框恒定像素虚线、PCB 描边全圆头、复用块 epro2 支持;建立 ref-diff 视觉回归管线(10 页样例像素 diff 基线)。
- **PCB 渲染保真批次(v0.2.2)**:客户端 2D 层序栈 pcbStackKey + 图层激活置顶/对面标注沉回/每面合成子层组;焊盘编号+网络名沿长轴双行居中、网络名统一墨色、顶/底/内层走线网络名、网络专色;铺铜暗化填充+全亮包边(fineness 包边宽、孤儿 POURED 只画包边);POLYGON 轮廓焊盘+开窗、ELLIPSE 圆头、槽孔 relativeAngle、padOffset 仅钻孔、通孔挂 MULTI 层、封装 Multi-Layer 填充=挖槽;阻焊/助焊窗 −1000 盖油哨兵;底面透视 BOTTOM_ALPHA + M_y·R(θ) 镜像 + 透板 twins;FONT 预矢量化字体轮廓文本 + CARC token。
- **桌面细节批次(v0.2.3)**:文档切换背景闪现(#bg-flash,场景内原子重绘);阻焊/助焊规则外扩按 mil 直取(修 40 倍外扩,#mask-rule-unit);网络名字号降至 5(mil 文档单位)。
- **启动提速(v0.2.4)**:宿主窗口先行显示(DPI 定尺居中、同色背景刷);WebView2 profile 固定 %LOCALAPPDATA% 并注入免首启参数。
- **原理图兼容批次(v0.3.1,X86 样例驱动)**:Y-up 坐标约定兼容(#x86-esch2-flip,旋转角手性随翻转共轭);属性文本严格按实例 `valueVisible` 显示、标志/端口网络名豁免(#attr-visibility);标题栏 `={@Key}` 系统属性动态合成与旧式图框值槽渲染(#titleblock-sysattrs);元件树按 symbolType 判定真元件、位号为空如实列出(#component-tree);原理图页切换免遮罩消除卡顿(#page-switch-jank);引脚名称/编号兼容大写历史键 NAME/NUMBER,非连接标识按引脚实时端点定位。
- **渲染修复批次(v0.3.2)**:镜像×旋转复合变换顺序(R·M 共轭,#mirror-rot-order);A3 等页面尺寸以图框实例属性为准、不信 CANVAS 声明(#border-size-override);多边形焊盘按封装坐标 as-is 绘制、消除切向偏移(#pad-polygon-ring)。
- **拾取与选中批次(v0.3.4)**:PCB 族画布点选激活层优先拾取(#pick-active-layer);导线/走线/弧/折线选中高亮沿实际绘制路径描亮、恒定像素虚线(#select-box-path)。
- **选框贴合与拾取精确批次(v0.3.5)**:旋转非零的元件/焊盘/丝印/文本选中框按实际旋转姿态渲染闭合四角折线(#select-box-rot);导线段逐段点到线段距离精确命中、L 形/斜线 bbox 空区不再误选、平行导线取更近者(#pick-precise);激活层拾取收紧为唯一过滤、不穿透回退(#pick-active-layer)。

## [0.2.0] - 2026-09-15

### Added
- M5 体验批次:中英双语 UI、纯图标工具栏 + 可输入缩放、面板宽/高拖拽、初始隐藏侧栏、右键平移、EasyEDA 品牌图标、桌面 exe 图标与版本属性、图层列表按使用习惯排序并翻译标准层名等 33 项反馈全部关闭。

## [0.1.0] - 2026-09-14

### Added
- 初始版本:工程加载(`.eprj3` 文件夹/zip、`.epro2`、单文档 `.esch2/.epcb2/.epan2`)、文档树/对象树/属性面板四件套交互(双向定位、指针中心缩放、右键平移)、原理图核心图元与 PCB/面板渲染(覆铜/焊盘/丝印保真、图层体系)、`dist/index.html` 单文件双击打开与拖放。
